특성					정적 메서드										비정적 메서드
소속					클래스에 소속 (클래스 레벨)			     			객체에 소속 (인스턴스 레벨)
호출 방법				클래스 이름으로 호출 가능 (ClassName.method)	객체를 통해서만 호출 가능 (object.method)
this 				사용	불가능 (객체와 무관)							가능 (호출한 객체를 참조)
접근 가능한 멤버			정적 멤버만 접근 가능						정적 멤버와 비정적 멤버 모두 접근 가능
메모리 사용				클래스가 로드될 때 메모리에 올라감				객체가 생성될 때 메모리에 올라감
주 용도				공용 기능 구현 (유틸리티 함수 등)				객체의 상태를 조작하거나 객체와 관련된 동작